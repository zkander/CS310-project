

class Paper:
       def __init__(self,confName: str, authorId: int,paperNo: int, title: str, abstract: str, keywords, coAuthorsNames,reviewed: bool, systemDecision: str, document):
              self.confName = confName
              self.authorId = authorId
              self.paperNo = paperNo
              self.title = title
              self.abstract = abstract
              self.keywords = keywords
              self.coAuthorsNames = coAuthorsNames
              self.reviewed = reviewed
              self.systemDecision = systemDecision
              self.document = document
              
       
       