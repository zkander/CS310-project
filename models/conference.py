

class Conference:
       def __init__ (self, confName, dates, submissionDeadline, confCode):
              self.confName = confName
              self.dates = dates
              self.submissionDeadline = submissionDeadline
              self.confCode = confCode
       
              